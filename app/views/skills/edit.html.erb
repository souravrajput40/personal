<div class="container-fluid my-5 border shadow-sm" style="background-color: rgba(255,99,98,255);">
  <div class="row text-center p-4 pb-0 pe-lg-0 py-lg-3 align-items-center border shadow-lg">
  	<div class="col-2">
  		<%= link_to ('<i class="fa fa-arrow-left"></i>').html_safe, skills_path, class:"mx-5 text-dark fs-2 align-items-start "%>	
  	</div>
  	<div class="col-8">
			<h2 class="text-center">Update Skill</h2>		
  	</div>
  </div>
</div> 

<div class="new-page-form">
		<%= form_with(model: @skill, url: skill_path, method: :put) do |form| %>
  <% if @skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% @skill.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :about %>
    <%= form.text_area :about %>
  </div>
  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

</div>

		